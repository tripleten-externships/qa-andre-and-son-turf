
import { test, expect } from '@playwright/test';

 

//test('Learn More links work', async ({ page }) => {
  /* visit the website */
    //await page.goto('/');
    //verify Turf Products text is visible
    //await expect(page.getByText('Turf Products')).toHaveText('Turf Products');
    //hover over Turf Products
    //await page.getByRole('menuitem', { name: 'Option 1' }).hover();
/* go to expected page */
    //await page.getByRole('menuitem', { name: 'Option 2' }).click();
    //await page.getByRole('button', { name: 'Turf Products' }).click();
    //await page.selectOption('#Turf-Products',  { label: 'Turf King Fertilizer' });
/* check that im in the expected page */
    //await (page.url()).toHaveUrl();


/* click on 'Learn More' */
   // await page.getByRole('link', { name: 'Learn More' }).click();